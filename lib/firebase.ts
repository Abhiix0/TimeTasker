/**
 * Firebase placeholder — replace this when adding Firebase.
 *
 * Steps to enable:
 * 1. `npm install firebase`
 * 2. Create a Firebase project at https://console.firebase.google.com
 * 3. Replace the exports below with real initialization:
 *
 *   import { initializeApp } from 'firebase/app'
 *   import { getFirestore } from 'firebase/firestore'
 *   import { getAuth } from 'firebase/auth'
 *
 *   const app = initializeApp({ ...your firebaseConfig })
 *   export const db = getFirestore(app)
 *   export const auth = getAuth(app)
 */

export const db = null   // TODO: replace with real Firestore instance
export const auth = null // TODO: replace with real Firebase Auth instance
